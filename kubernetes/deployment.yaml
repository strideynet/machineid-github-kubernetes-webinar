apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: colormatic
  name: colormatic
  namespace: colormatic
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: colormatic
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
      app.kubernetes.io/name: colormatic
    spec:
      containers:
        - image: colormatic
          name: colormatic
          ports:
            - containerPort: 9090
              protocol: TCP
          resources: {}
      restartPolicy: Always